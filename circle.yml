machine:
  node:
    version: "8.9.0"

dependencies:
  cache_directories:
    - node_modules
    - ~/.electron
    - ~/.cache/electron-builder
    - ~/.cache/yarn

  pre:
    - npm install -g yarn

  override:
    - yarn install --force

test:
  override:
    - yarn run ci