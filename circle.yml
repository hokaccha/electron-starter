machine:
  node:
    version: "8.9.0"

dependencies:
  cache_directories:
    - node_modules
    - ~/.electron
    - ~/.cache/yarn

  pre:
    - npm install -g yarn

  override:
    - yarn install --force

test:
  cache_directories:
    - ~/.cache/electron-builder
  override:
    - yarn run ci